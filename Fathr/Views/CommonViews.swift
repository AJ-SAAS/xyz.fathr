import SwiftUI

// Placeholder for shared views (StatusBox removed to avoid conflict)
struct PlaceholderView: View {
    var body: some View {
        Text("Shared View Placeholder")
    }
}

#Preview {
    PlaceholderView()
}
